export interface HeaderProps {
    className?: string;
    header: header;
}

export interface header{
    home: string;
    about: string;
    skills: string;
    project: string;
    contacts: string;
}